[package]
name = "devtoolkit-server"
version = "1.0.0"
edition = "2021"

[dependencies]
dev-toolkit = { path = ".." }
axum = "0.7"
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.6", features = ["cors"] }
